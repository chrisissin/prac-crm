development:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] || "dev_secret_key_base_change_in_production_32chars_min" %>

test:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] || "test_secret_key_base" %>

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
